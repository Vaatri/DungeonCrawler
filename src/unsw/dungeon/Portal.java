package unsw.dungeon;

public class Portal extends Entity {
	public Portal(int x, int y) {
        super(x, y);
    }
	
	public void teleport(Player p) {
		//teleport the player to a linked portal.
		//set player x and y value
	}
}
